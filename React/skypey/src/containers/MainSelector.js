const activeUserIdSel = (state) => state.activeUserId;
const contactsSel = (state) => state.contacts;
const messagesSel = (state) => state.messages;
const usersSel = (state) => state.user;
const typingSel = (state) => state.typing;

export { activeUserIdSel, contactsSel, messagesSel, usersSel, typingSel };
